--- a/llvm/lib/Target/Xtensa/XtensaBRegFixupPass.cpp
+++ b/llvm/lib/Target/Xtensa/XtensaBRegFixupPass.cpp
@@ -94,6 +94,7 @@
   unsigned Opcode = MI->getOpcode();
 
   switch (Opcode) {
+  case Xtensa::MOVBA4_P2:
   case Xtensa::MOVBA2_P2:
   case Xtensa::MOVBA_P2: {
 
@@ -108,6 +109,10 @@
     case Xtensa::MOVBA2_P2:
       RegBase = Xtensa::B0_B1;
       Arity = 2;
+      break;
+    case Xtensa::MOVBA4_P2:
+      RegBase = Xtensa::B0_B1_B2_B3;
+      Arity = 4;
       break;
     default:
       llvm_unreachable("Unknown MOVBA opcode");
@@ -124,34 +129,42 @@
       WSR       BREG, Dst2
       */
     // TODO: Mask SRC, e.g. by EXTUI
-    MachineOperand Breg = MI->getOperand(0);
-    MachineOperand Dst1 = MI->getOperand(1);
-    MachineOperand Dst2 = MI->getOperand(2);
-    MachineOperand Src = MI->getOperand(3);
+    Register Breg = MI->getOperand(0).getReg();
+    Register Dst1 = MI->getOperand(1).getReg();
+    Register Dst2 = MI->getOperand(2).getReg();
+    Register Src = MI->getOperand(3).getReg();
     DebugLoc DL = MI->getDebugLoc();
-    unsigned RegNo = Breg.getReg().id() - RegBase;
+    unsigned RegNo = Breg.id() - RegBase;
 
     int64_t BaseMask = (1 << Arity) - 1;
     int64_t Mask = 0xffff & (~(BaseMask << (RegNo * Arity)));
 
-    MachineInstrBuilder MIB = BuildMI(MBB, MI, DL, TII.get(Xtensa::RSR)).add(Dst1).addReg(Xtensa::BREG);
-
-    TII.buildLoadImmediate(MBB, MI, Dst2.getReg(), Mask);
-
-    BuildMI(MBB, MI, DL, TII.get(Xtensa::AND)).add(Dst2).add(Dst2).add(Dst1);
+    [[maybe_unused]] MachineInstrBuilder MIB =
+        BuildMI(MBB, MI, DL, TII.get(Xtensa::RSR), Dst1).addReg(Xtensa::BREG);
+
+    TII.buildLoadImmediate(MBB, MI, Dst2, Mask);
+
+    BuildMI(MBB, MI, DL, TII.get(Xtensa::AND), Dst2)
+        .addReg(Dst2)
+        .addReg(Dst1);
 
     if (RegNo > 0) {
       BuildMI(MBB, MI, DL, TII.get(Xtensa::SLLI))
-          .add(Dst1)
-          .add(Src)
-          .addImm(RegNo);
+          .addReg(Dst1)
+          .addReg(Src)
+          .addImm(RegNo * Arity);
     } else {
-      Dst1.setReg(Src.getReg());
-    }
-
-    BuildMI(MBB, MI, DL, TII.get(Xtensa::OR)).add(Dst2).add(Dst2).add(Dst1);
-
-    BuildMI(MBB, MI, DL, TII.get(Xtensa::WSR)).addReg(Xtensa::BREG).add(Dst2);
+      Dst1 = Src;
+    }
+
+    BuildMI(MBB, MI, DL, TII.get(Xtensa::OR), Dst2)
+        .addReg(Dst2)
+        .addReg(Dst1);
+
+    BuildMI(MBB, MI, DL, TII.get(Xtensa::WSR))
+        .addReg(Xtensa::BREG, RegState::Define)
+        .addReg(Dst2)
+        .addDef(Breg, RegState::Implicit);
 
     LLVM_DEBUG(dbgs() << " Fixed MOVBA_P2: " << *MIB);
     MBB.erase_instr(MI);
--- a/llvm/test/CodeGen/Xtensa/xtensa-xtbool-convert.ll
+++ b/llvm/test/CodeGen/Xtensa/xtensa-xtbool-convert.ll
@@ -1,5 +1,5 @@
 ; NOTE: Assertions have been autogenerated by utils/update_llc_test_checks.py UTC_ARGS: --version 5
-; RUN: llc -mtriple=xtensa -mcpu=esp32 %s -o - | FileCheck %s
+; RUN: llc -verify-machineinstrs -mtriple=xtensa -mcpu=esp32 %s -o - | FileCheck %s
 
 define <1 x i1> @test_xtbool_trunc(i32 %a)  {
 ; CHECK-LABEL: test_xtbool_trunc:
@@ -14,6 +14,56 @@
   %trunc = trunc i32 %a to i1
   %vec = insertelement <1 x i1> poison, i1 %trunc, i64 0
   ret <1 x i1> %vec
+}
+
+define <1 x i1> @test_xtbool_const_ret_v1() {
+; CHECK-LABEL: test_xtbool_const_ret_v1:
+; CHECK:       # %bb.0:
+; CHECK-NEXT:    entry a1, 48
+; CHECK-NEXT:    movi.n a8, 1
+; CHECK-NEXT:    rsr a9, br
+; CHECK-NEXT:    l32r a10, .LCPI1_0
+; CHECK-NEXT:    and a10, a10, a9
+; CHECK-NEXT:    or a10, a10, a8
+; CHECK-NEXT:    wsr a10, br
+; CHECK-NEXT:    retw.n
+  ret <1 x i1> <i1 true>
+}
+
+define <2 x i1> @test_xtbool_const_ret_v2() {
+; CHECK-LABEL: test_xtbool_const_ret_v2:
+; CHECK:       # %bb.0:
+; CHECK-NEXT:    entry a1, 48
+; CHECK-NEXT:    movi.n a8, 1
+; CHECK-NEXT:    rsr a9, br
+; CHECK-NEXT:    l32r a10, .LCPI2_0
+; CHECK-NEXT:    and a10, a10, a9
+; CHECK-NEXT:    or a10, a10, a8
+; CHECK-NEXT:    wsr a10, br
+; CHECK-NEXT:    rsr a8, br
+; CHECK-NEXT:    extui a8, a8, 0, 2
+; CHECK-NEXT:    s8i a8, a1, 7
+; CHECK-NEXT:    l8ui a2, a1, 7
+; CHECK-NEXT:    retw.n
+  ret <2 x i1> <i1 true, i1 false>
+}
+
+define <4 x i1> @test_xtbool_const_ret_v4() {
+; CHECK-LABEL: test_xtbool_const_ret_v4:
+; CHECK:       # %bb.0:
+; CHECK-NEXT:    entry a1, 48
+; CHECK-NEXT:    movi.n a8, 5
+; CHECK-NEXT:    rsr a9, br
+; CHECK-NEXT:    l32r a10, .LCPI3_0
+; CHECK-NEXT:    and a10, a10, a9
+; CHECK-NEXT:    or a10, a10, a8
+; CHECK-NEXT:    wsr a10, br
+; CHECK-NEXT:    rsr a8, br
+; CHECK-NEXT:    extui a8, a8, 0, 4
+; CHECK-NEXT:    s8i a8, a1, 7
+; CHECK-NEXT:    l8ui a2, a1, 7
+; CHECK-NEXT:    retw.n
+  ret <4 x i1> <i1 true, i1 false, i1 true, i1 false>
 }
 
 define i32 @test_xtbool_zext(<1 x i1> %b) {
